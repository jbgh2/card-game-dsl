---
term: Move Type
definition: A named, parameterized player action with a `when:` guard and an effect. Keyword and term settled: with "move" owning the player-action family, `move_type` reads as compositional English; `action_type` would manufacture an Interop false friend.
layer: kernel
status: canonical
reserved: false
home: `n.MoveTypeDef`
see: []
retired_spellings: []
findings: []
---

**Interop.** OpenSpiel calls it **verb** (encoding-granularity name; `<card>`/`<int>`/`<combo>` sentinels) — translated in `encoding.verb_of`.
