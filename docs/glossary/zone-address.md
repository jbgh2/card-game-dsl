---
term: Zone Address
definition: The `(name, key)` pair identifying one zone instance — `("hand", 2)`, or `("deck", None)` for a family-less zone. Named because it is one identity, not two arguments: its label form (`hand[2]`) is currently formatted at three separate sites. Becomes a dataclass when-touched, which collapses that triplication.
layer: kernel
status: canonical
reserved: false
home: `state.ZoneStore.locate`
see: []
retired_spellings: []
findings: []
---
