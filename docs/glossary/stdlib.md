---
term: Stdlib
definition: The standard library: the layer **written in the language itself** — `stdlib/rules.cardlang` and what grows beside it. This is the word's only meaning; endpoint is `cardlang/stdlib/` holding only `.cardlang` files + loader, pinnable. Native functions are **builtins**; game-local native code is a **Primitive**; registry data are **kernel tables**. Spec: issue #203.
layer: kernel
status: canonical
reserved: false
home: `cardlang/stdlib/`
see: []
retired_spellings: []
findings: []
---
