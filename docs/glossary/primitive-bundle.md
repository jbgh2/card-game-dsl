---
term: Primitive Bundle
definition: The pair every Primitive receives: the **facts** (`EngineFacts`) and its declared **reads** (`GameReads`). Named because it is one thing passed together, restated as `(facts, gr)` at every primitive signature; it becomes a NamedTuple when the sidecar design lands, and the two halves keep their own names either way.
layer: kernel
status: canonical
reserved: false
home: `narrowing.py`, `reads.py`
see: []
retired_spellings: []
findings: []
---
