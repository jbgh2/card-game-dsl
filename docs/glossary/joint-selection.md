---
term: Joint Selection
definition: The `where jointly <pred>` form of a chosen [[transfer]]: one decision over the subsets of the source that satisfy the predicate *together*, binding `cards`. Its candidates are card subsets, numbered by the subset codec. Spelled `joint` in the `asked` [[observation-event]]. Not "combination", which names the climbing form's play and the action block both share, and not bare "subset", which is reserved.
layer: kernel
status: canonical
reserved: false
home: `execute._select_joint`
see: [transfer, observation-event]
retired_spellings: []
findings: []
---
