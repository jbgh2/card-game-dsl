---
term: Resolve
definition: Name resolution: classifying every name and checking structural references. A `_resolve_*` name is legal only for a function that classifies names or resolves them; a validator is `_check_*`, and a function that mints a domain says `_mint_*`. Applied when-touched — the surviving `_resolve_*` validators in `resolve.py` rename as their sites are next edited.
layer: compiler
status: canonical
reserved: false
home: `resolve.py`
see: []
retired_spellings: []
findings: []
---
