# Direction review — 2026-09-22

Eighth run. The window is the 20 PRs merged after verdict 7's snapshot
(PR #660's merge, `a48e1107`, 2026-09-15 20:27 UTC), selected by merge
time: #671 through #748, running to 2026-09-22. PRs #661, #663, #667
and #669 merged before that snapshot and belong to verdict 7. Issues
are counted from #696. Measurements were taken on 2026-09-22 at
`8b283099` (PR #748's merge). The next window starts after #748.

## Verdict 7's scorecard

- (1) #648 closed, and #546 with it: **MET** (PR #745).
- (2) Three Pinochle R1s closed on Lane C: **MET and exceeded**. All
  fourteen parts closed, in PR #748, which landed in Merge Lane B.
- (3) #486 taken, or its thread saying why it is parked: **MISSED**.
  The cause was a mis-rank; see check 3.
- (4) #693 merged: **MISSED**. It is Merge Lane A, the operator's.
- Both amendments were ruled and landed: A as #695 and B as #697.

## The shape

**This is the first window in which wrong corpus games closed faster than
they arrived, and the Active Epic is the reason.** Twenty-two R1s closed.
Three games now play the game their page describes: Hearts (PR #710),
Seven-Card Stud (seven parts, PRs #731, #745 and #747) and Pinochle
(fourteen parts, PR #748). While Pinochle's P1s were loose rows, none of
them closed in five days at the top of the front. Once they were a
milestone, all of them closed within two. Twenty-four wrong-game issues
closed. Nine arrived in the same window: Tichu's seven, plus #738 and
#739, which Stud's epic absorbed.

The cost of the window is on the harness side. The R4 share doubled,
from 13% to 26%, and most of the new R4s are the harness filing about
itself: #732 to #736 from the Active Epic's first test round, and #750.
This is the second sighting of the "filed en route on machinery" stream
that verdict 6 watched for. It gets no amendment. The harm verdict 6
feared was R4 work displacing R1 work, and the Priority Tier now
prevents that by construction: **0 of 94 open R4s carry a tier**
(measured). An untiered row cannot outrank a tiered one.

The window also shows what a faithful game costs in Python.
`cardlang/runtime/pinochle.py` grew from 71 to 176 lines, because
Pagat's meld table landed as Primitive code. Correcting a corpus game
adds to epic #248's debt instead of retiring any of it, and the gap that
owns the meld code (#254) is language-frontier work for the operator.
This is named here, not reallocated.

## The checks

**(1) Severity mix.** 39 issues were filed, not counting epics: 14 R1,
9 R2, 6 R3 and 10 R4. The designer-reachable share fell from 75% to
59%. The R1s came from witnesses:
- Hearts (3), Stud (2) and Tichu (7, counting #703);
- the play epic (#698) and the Skat read (#728).

Codex ran 27 rounds and posted 9 P1s and 37 P2s. Every P1 landed on an
execution or study PR: #671, #683, #704, #716, #745 and #748. The seven
harness, tool and doctrine PRs took one round each and produced no P1,
so amendment B's round cap held.

**(2) Machinery vs witness: 10 execution, 2 study, 6 harness or tool,
2 docs.**
- Execution: #681, #683, #702, #704, #710, #716, #731, #745, #747,
  #748.
- Study: #671, #705.
- Harness: #694, #695, #697, #729, #737. Tool: #706.
- Docs: #719, #720.

The harness count is the highest yet, and it paid out: #729 is the
mechanism behind the shape above. The corpus stays at 34 files, with no
new game since 2026-09-06, because this window corrected games instead
of adding them.

**(3) Aging, read by body.**
- **#154, #158**: correctly parked, and queued in #143.
- **#486**: mis-ranked, not neglected. Verdict 7's triage gave it
  `priority:P1`, and that kept it at or near the head of the front for
  three windows. It is internal to the rig. Checked against the study:
  - the cheat_gap endpoint is the LLM seat's selection contrast over
    challenge windows;
  - those windows render through the correct `open_window` sentence;
  - the mislabelled sentence reaches only the seat's own play-count
    decisions, so the confound is at most indirect.

  The tier came off under the default rule, and #662, the same class,
  lost its tier too. Both reasons are on the issues.
- **#592, #596, #612** (15 days): neglected by shape, not by choice.
  They were three parts with no unit, and they are now an Active Epic.
- **Tichu** (#703, #721 to #727): two to four days old, and the head of
  #143.

**(4) Inflow vs closure.** 39 issues were filed and 33 closed after the
snapshot, and 322 are open. By class:
- **Corpus fidelity** closed 24 and received 9 (above).
- **Epic #613 (play)** has closed. Its successors (#698, #711, #712,
  #714, #715, #717) arrive with no epic, and only #698 among them is R1.
- **Epic #248** is unchanged at 18 game modules and has grown in lines
  (see the shape).

**(5) The doctrine's own cost.** Both of verdict 7's amendments changed
outcomes. The tier is what keeps the R4 stream harmless, and no
machinery PR ran past one round. The one gate defect this window is
#750: the sweep aborts in the gap between an epic's close and its
milestone's close, and it fired live on #690. **Until #750 lands, the
review closes a milestone before its epic.** That is an operating order
for this role, not an amendment. CLAUDE.md grew from 451 to 457 lines.

**(6) Surface nobody uses.** `python -m tools.dead_surface` at
`8b283099` lists 25 of 199 rules and 13 of 124 keywords. One row is new:
`always`, the `applies_when` wildcard, whose only use left with Hearts
(PR #710). **Retire**: a rule with no `applies_when` says the same
thing. It joins #693, and the lookahead terminals it appears in are named
on that issue. The `order` rule row has left the report while its
keyword stays listed, which is flagged on #693 for the taker. Every
other row keeps verdict 7's decision and is not re-offered.

## Reallocation moves

1. **Two Active Epics are open, both Merge Lane B.**
   - Milestone 4: #312 with #281, due 2026-10-02. Its finish line is the
     epic's own completion test, a verified reddening plant for each of
     its four classes, not "#281 closes".
   - Milestone 5: the new epic #751 with #612, #592 and #596, due
     2026-10-09. #596 carries a decision the operator makes at pickup.
2. **Tichu is #143's head and the next promotion.** Its parts carry
   `priority:P1` or `P2` by the default rule, so they sit directly under
   the two active rows, and each part says on its thread to take the
   unit whole and to probe first. This review chose to let the tier state
   the fact rather than hide the parts until promotion.
3. **#143 carries no Merge Lane A unit.** Per #737's ruling, the retire
   set moved to #143's operator-gated paragraph.

## Triage

- `priority:P1`: #721 to #727 (Tichu wrong against its source), #740
  (an unoffered library move type silently renumbers every consumer),
  #746 (a second wager size dropped without a message).
- `priority:P2`: #698 (a looping game blamed on its rules), #703 (Tichu
  refused at the table).
- Tier removed: #486 and #662 (internal to the rig; see check 3).
- Untiered on purpose:
  - #749: Merge Lane A surface; the reason is on the issue.
  - #742: all three drift rows are closed; the reason is on the issue.
  - #728: a read.
  - #741, #744 and #750: not designer-facing.

The re-run of `tools/ready-front.sh` shows the two ACTIVE rows first,
then the Tichu P1s. Every move above is a selectable row.

## Raised with the operator, not offered here

Three items go to the operator in conversation and are not re-offered in
this verdict:
- #486's three windows at the head of the front;
- the 2026-07-25 R2 batch (#96 to #146), on its third sighting and still
  unread against the tree;
- the retire set, ruled 2026-09-06 and executed on none of its six
  issues, with #693 beside it.

A later verdict that finds any of them unchanged records the
conversation's outcome. It does not re-flag them.

## Next window's pass conditions (each selectable)

(1) #281 closed inside milestone 4, with its plant verified; (2) #612
closed; (3) Tichu's corrected file probed at playout scale, with the
result posted on #703.

## What a green does not prove

Every count is a snapshot from `gh` at `8b283099`. Judgment calls:
- **Filed and open counts don't reconcile.** The filed count starts at
  #696, and #689 to #691 fall between two snapshots, so 304 open
  (verdict 7) plus 39 filed minus 33 closed does not give 322.
- **The PR classification** is judged, and #706 and #719 sit on
  boundaries.
- **The Codex counts** read P-badges on review comments by the bot's
  login, so they miss any finding posted as a plain comment.
- **"Corpus fidelity" as a class** is counted by hand from titles.
- **The game-module census** is a hand list of the files under
  `cardlang/runtime/` named for a game.
