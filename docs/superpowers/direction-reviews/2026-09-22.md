# Direction review — 2026-09-22

Eighth run. The window is the 24 PRs merged after #660, #661 through #748
by merge order, from 2026-09-08 to 2026-09-22. Issues are counted from
#694. Measurements were taken on 2026-09-22 at `8b283099` (PR #748's
merge). The next window starts after #748.

## The shape

**This is the first window in which wrong corpus games closed faster than
they arrived, and the Active Epic is the reason.** Twenty-two R1s closed.
Three games now play the game their page describes: Hearts (PR #710),
Seven-Card Stud (seven parts, PRs #731, #745 and #747) and Pinochle
(fourteen parts, PR #748). When Pinochle's P1s were loose rows, none of
them closed in five days at the top of the front. Once they were a
milestone, all of them closed in two. Nine wrong-game issues arrived in
the same window: Tichu's seven, plus #738 and #739, which Stud's epic
absorbed. Twenty-four closed.

The cost of the window is on the harness side. The R4 share doubled, from
13% to 26%, and most of the new R4s are the harness filing about itself:
#732 to #736 from the Active Epic's first test round, and #750. This is
the second sighting of the "filed en route on machinery" stream that
verdict 6 watched for. It gets no amendment. The harm verdict 6 feared
was R4 work displacing R1 work, and the Priority Tier now prevents that
by construction: **0 of 94 open R4s carry a tier** (measured). An
untiered row cannot outrank a tiered one.

The window also shows what a faithful game costs in Python.
`cardlang/runtime/pinochle.py` grew from 71 to 176 lines because
Pagat's meld table landed as Primitive code. Correcting a corpus game
adds to epic #248's debt instead of retiring any of it, and the gap that
owns this (#254, melds) is language frontier work for the operator. This
is named here, not reallocated.

## The checks

**(1) Severity mix.** 39 issues were filed (not counting epics): 14 R1,
9 R2, 6 R3 and 10 R4. The designer-reachable share fell from 75% to 59%.
The R1s came from witnesses: Hearts (3), Stud (2), Tichu (6), and the
play and Skat reads. Codex ran 29 rounds and posted 12 P1s and 39 P2s.
Every P1 landed on an execution or study PR (#663, #671, #683, #704,
#716, #745, #748). The seven harness and doctrine PRs took one round each
and produced no P1. Amendment B's round cap held.

**(2) Machinery vs witness: 13 execution, 3 study, 6 harness or tool,
2 docs.**
- Execution: #661, #667, #669, #681, #683, #702, #704, #710, #716,
  #731, #745, #747, #748.
- Study: #663, #671, #705.
- Harness: #694, #695, #697, #729, #737. Tool: #706.
- Docs: #719, #720.

The harness count is the highest yet, and it paid out: #729 is the
mechanism behind the shape above. The corpus stays at 34 files, with no
new game since 2026-09-06, because this window corrected games instead
of adding them.

**(3) Aging, read by body.**
- #154, #158: correctly parked; they are queued in #143.
- #486: its third window at or near the head of the front, untaken and
  with no comment. Reading the body against the tree, it is not
  internal-only in consequence. The cheat_gap study's LLM cells ran
  rendered state, so every count decision reached the sentence this
  issue names, and `REPORT_CHEAT_GAP.md` does not mention it. The P1
  stays. This goes to the operator as a conversation (recorded on the
  issue).
- #592, #596, #612 (15 days): neglected by shape rather than by choice.
  Three parts with no unit. They are now an Active Epic.
- Tichu (#703, #721 to #727): two to four days old, and the head of
  #143.

**(4) Inflow vs closure.** 39 issues were filed and 34 closed. There are
322 open, up from 304. By class: corpus fidelity closed 24 and received 9
(above). Epic #613 (play) closed. Its successors (#698, #711, #712,
#714, #715, #717) arrive with no epic, and none is R1 except #698.
Epic #248 is unchanged at 18 game modules and has grown in lines (see
the shape).

**(5) The doctrine's own cost.** Both amendments from verdict 7 landed
(#695, #697) and both changed outcomes. The tier is what keeps the R4
stream harmless, and no machinery PR ran past one round. The one gate
defect this window is #750. The sweep aborts between an epic's close and
its milestone's close. It fired live on #690. **Until #750 lands, the
review closes a milestone before its epic.** That is an operating order
for this role, not an amendment. CLAUDE.md grew from 451 to 457 lines.

**(6) Surface nobody uses.** `python -m tools.dead_surface` at
`8b283099` lists 25 of 199 rules and 13 of 124 keywords. One row is new:
`always`, the `applies_when` wildcard. Its only use left with Hearts
(PR #710). **Retire**: a rule with no `applies_when` says the same
thing. It joins #693, and the lookahead terminals are named on that
issue. The `order` rule row has left the report while its keyword stays
listed, which is flagged on #693 for the taker. Every other row keeps
verdict 7's decision and is not re-offered.

## Reallocation moves

1. **Two Active Epics are open, both Merge Lane B.**
   - Milestone 4: #312 with #281, due 2026-10-02. Its finish line is the
     epic's own completion test, a verified reddening plant for each of
     its four classes, not "#281 closes".
   - Milestone 5: the new epic #751 with #612, #592 and #596, due
     2026-10-09. #596 carries a decision the operator makes at pickup.
2. **Tichu is #143's head and the next promotion.** Its parts carry
   `priority:P1`/`P2` by the default rule, so they sit directly under the
   two active rows. Each part says on its thread to take the unit whole
   and probe first. This is the choice between letting the tier state
   the fact and hiding the parts until promotion; the verdict takes the
   first.
3. **#143 carries no Merge Lane A unit.** The retire set moved to its
   operator-gated paragraph, per #737's ruling.

## Triage

- `priority:P1`: #721 to #727 (Tichu wrong against its source), #740
  (an unoffered library move type silently renumbers every consumer),
  #746 (a second wager size is dropped without a message).
- `priority:P2`: #698 (a looping game blamed on its rules), #703 (Tichu
  refused at the table).
- Untiered on purpose: #749 (Merge Lane A surface, reason on the issue),
  #742 (no wrong game standing that this review verified; the one
  unverified row is noted on the issue), #728 (a read), #741, #744 and
  #750 (not designer-facing).

The re-run of `tools/ready-front.sh` shows the two ACTIVE rows, then
#486, then the Tichu P1s. Every move above is a selectable row.

## Next window's pass conditions (each selectable)

(1) #281 closed inside milestone 4, with its plant verified; (2) #612
closed; (3) Tichu's corrected file probed at playout scale, with the
result posted on #703.

## What a green does not prove

Every count is a snapshot from `gh` at `8b283099`. Judgment calls:
- the PR classification (#667 and #706 sit on the boundary);
- the Codex counts, which read P-badges on review comments by the bot's
  login and miss any finding posted as a plain comment;
- "corpus fidelity" as a class, which is counted by hand from titles;
- the game-module census, which is a hand list of the files under
  `cardlang/runtime/` named for a game.
