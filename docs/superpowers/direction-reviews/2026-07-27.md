# Direction review — 2026-07-27

First run. Window: the 15 PRs merged 2026-07-24 → 2026-07-28 — #92
(breakthrough) first, #172 last by merge time; PR numbers are not
merge-ordered (#182 merged before #172), so the next window starts after
#172's merge, not after the highest number. Next review due after ~10 more
merges.

## The shape

**Early drift, honestly recorded.** Of 15 PRs: 1 witness (#92,
breakthrough + native differential, at the window's open), 2 execution-layer
fixes (#94 wrong-seat returns, #169 climb-leader kernel fix), 7 enforcement
(#93, #148, #160, #163, #166, #172, #182), 5 process/hygiene (#151, #156,
#159, #161, #164). Fourteen consecutive PRs since the last witness landed no
new execution surface — no game, no differential, no proof generalization.

The finding stream followed the work. First half of the window: 3 R1 + 5 R2
filed, designer-surface findings. Second half: 1 R2, 5 R3 (all on deferred
surface), 7 R4 — the R4s generated by machinery examining machinery (#166's
guard witnesses spawned #165 and #171, a census of the census; #163's
evaporation wall spawned #162). Both drift signatures present. Mitigations:
the R2 decline is partly the surface genuinely closing (#93's choke point,
#182's rule-surface totality), the machinery spend was #143's own then-top
items being drained (now trimmed), and the guarded-once cap landed mid-window
and is holding — the new meta-records were filed, not fixed.

The window's one silent-wrong-answer-on-target defect (#94) was found by a
reviewer pulling an adjacent thread and confirmed by executing the case —
not by any of the window's new enforcement. CLAUDE.md's ratio claim stands.

## The other checks

- **Aging (check 3):** #25 — generalize the readiness proofs — is R2, open
  since 2026-07-04, 23 days: three weeks older than everything else, and it
  IS the OpenSpiel promise. The clean allocation failure of the window. The
  R1s (#105, #126, #154, #157, #158) are all under 3 days old — fine, but
  they are all execution-quality issues and must not slip past next window.
- **Inflow vs closure (check 4):** 8 closed vs ~28 new findings filed.
  Library-contract class: 6+ in (epic #181's children), 0 out — refactor
  correctly gated on the second-library witness, which is #143 item 1; the
  class is outrun only if item 1 keeps not starting. Meta-test class:
  3 closed (#83, #149, #150), 3 spawned (#162, #165, #171) — self-sustaining
  at depth 3 (witnesses for guards for pins); parked, see below.
- **Doctrine cost (check 5):** #166 — three review rounds plus class ledgers
  on a change with zero behavioral delta, whose findings were all R4. The
  adversarial audit's yield on scaffolding-only PRs is ledger-prose
  corrections, not defects (#172's five "all reasons, no conclusions").
  Narrowed by amendment, below. Sweep compliance: #171 was filed with no
  reachability label; fixed (R4).

## Reallocation moves

1. **Land execution next, where #143 already points.** Item 1 (green-lane
   second library — now explicitly the witness event for epic #181 too) and
   item 3 with #25 lifted out of its parenthetical (oldest R2) and #109's
   six frozen fuzz findings unfrozen. Pass condition for the next review: at
   least one of {second library, new game + differential, #25} merged.
2. **Park the scaffolding-on-scaffolding class.** #162, #165, #171 added to
   item 6 as wait-for-a-bite; no standalone meta-test PRs.
3. **Light gate for scaffolding-only changes** — doctrine amendment applied
   to decisions.md "The machinery is guarded once": grid stays, one review
   round, no standalone adversarial claim-audit; full cadence reserved for
   deltas a designer, the corpus, or a proof can meet.

## Applied in place

- Issue #143: item 1 + item 3 + item 6 edits above.
- Issue #171: `reachability:R4` added.
- decisions.md: the process-cost paragraph appended to "The machinery is
  guarded once".
- cardlang-code-review skill: effort tier keyed to the scaffolding test
  (the amendment's mechanization — Thorough is unreachable for
  scaffolding-only diffs).
