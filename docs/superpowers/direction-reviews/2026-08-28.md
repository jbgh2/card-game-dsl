# Direction review — 2026-08-28

Fifth run, on cadence. Window: every PR merged after #426 except #428 (the
previous verdict itself) — #430, #433, #435, #439, #440, #443, #446, #448,
#456, #459, #465. 11 PRs, 2026-08-25 to 2026-08-28. Issues counted from #429
(the first filed after verdict 4's last derivation). Next window starts after
#465. Measurements taken 2026-08-28 at `c3b9b47`.

## The shape

**The R1 shelf did not just break — it emptied and refilled from a better
source. And this review's own instrument is now ranking R4 tickets above the
R1s that replaced it.**

Seven R1s closed in four days (#105, #126, #157, #192, #431, #436, #452) —
the largest R1 closure any window has recorded, and four of them were the
30-day residents verdict 4 was still ranking. Six new R1s arrived, and they
are a different kind of finding: **our games are not the games.** #453 Cheat
refuses a legal five-card play, #454 French Tarot drops a discard the real
game shows, #455 Oh Hell's comment makes a language gap read as optional,
#442 a short all-in re-opens betting the rules close. The designer-reachable
ratio went from 30% to **64%** (16 of 25 non-epic: 9 R1 / 7 R2 / 3 R3 / 6 R4,
plus epic #445).

That is not drift. Verdict 4's two named engines both fired: PR #446 turned
the operator's ruling into doctrine and the doctrine immediately swept the
corpus for its own violations (epic #445, three R1s, same day); the framing
check run for #157 produced four accepted-but-ignored surface bugs (#461-#464),
three confirmed by execution.

**The allocation failure is mechanical and this review caused it.**
`tools/ready-front.sh` derives an issue's rank from first-appearance order of
`#N` in #143's body. Verdict 4 wrote #378-#386 into item 3, giving those six
orphaned-surface tickets ranks 16-23; it wrote item 1's R1 list as "#154 rides
item 4 and #158 item 6". So today the sweep ranks **six R4/R3 machinery
tickets ahead of seven R1s that are READY and carry no rank at all** (#357,
#441, #442, #444, #453, #454, #455). Nothing is blocking them. The list simply
does not mention them. Fixed below.

## The checks

**(1) Severity mix — the healthiest recorded, and the source is right.** 9 R1
filed against verdict 4's 1. Every one traces to execution or to an external
authority, none to machinery inspecting itself. The R4s are 6 of 25 and are
byproducts (#432, #437, #451, #460, #467), not a track.

**(2) Machinery vs witness — 7 execution surface to 4 doctrine/harness**
(#433, #435, #439, #440, #443, #459, #465 against #430, #446, #448, #456).
#443 (a test-only correction to a witness's oracle) and #446 (doctrine that
paid out in corpus findings within hours) sit awkwardly on either side; the
conclusion does not move whichever way they go, so they are named rather than
reconciled.

The exception verdict 4 named holds and has aged: **the corpus has not grown
in 25 days.** Last new game 2026-08-03. But the window changed what that
means — see move 1.

**(3) Aging by reachability.** 9 open R1s. Only three predate the window:
#154 and #158 (33 days, both attributed to #143 items 4 and 6) and **#357 (12
days, French Tarot against Pagat, unranked)**. #25 came off the conversation
list last verdict and is still open at rank 7 — a selection fact, not a
hygiene complaint, as promised.

**(4) Inflow vs closure by class.** 26 filed, 10 closed, net +16; 199 open.

- **Epic #445 is the window's new class and it is 1 of 5 done** (#452, Bridge's
  dummy, via PR #459). Its own ledger says the confession-grep finds only games
  that *admit* a harness motivation, and that a game bent silently is inside the
  class and outside the matcher.
- **Six distinct corpus games now have a filed rules discrepancy against
  Pagat** — Bridge (#415, #452), Stud (#431), Hold'em (#442), Cheat (#453),
  French Tarot (#357, #454), and 500 at R3 (#106). Each was filed
  opportunistically: the provenance lines read "split from #436 after fetching
  Pagat live", "swept while ruling #415", "found by language work". No hit rate
  is available from this and none is claimed — a fetch that finds the game
  correct files nothing, so the denominator does not exist. What is derivable is
  the absence of a *systematic* pass, and epic #445 states it in its own words:
  the differential against source is "Recorded, not scheduled". The rest of the
  traditional corpus — 31 games less the abstract and synthetic ones
  (breakthrough, tic-tac-toe, klondike, freecell, coup, kuhn-poker, leduc-poker)
  — is **unmeasured**, and the unmeasured-ness is the argument, not a prediction
  about what is in there.
- **The ledger-format migration stopped.** 84 old-format modules at the
  window's base (`9602755`) and 84 at HEAD — zero migrated. New-format went
  12 to 14, both newly-born modules. Verdict 4's move 2 offered "batch it or
  bound it"; what happened is the bound, taken silently. Ratified in #143 below
  so it reads as a decision rather than a lapse.
- **Epic #248 did not move.** 16 game modules under `cardlang/runtime/`,
  unchanged. Its two cheapest ticks (#255, #142) are still untaken.

**(5) The doctrine's own cost.** The instance is **#443**: the surface-totality
grid for #440 was authored red-first, as doctrine requires, against a
*misremembered* raise rule — and red-first authorship made the wrong
expectation look authoritative enough to blame the library for disagreeing with
it. That is the known blind spot (the framing check cannot verify the expected
column) landing on the one artifact class the repo treats as ground truth. It
does not want a gate narrowed; it wants the expected column sourced, which is
move 1. **No amendment is drafted this round.**

CLAUDE.md is worth one measured line, offered as a rate rather than a finding:
307 lines (07-27), 367, 366, 405, **448** (HEAD) — 43 added in this window, the
fastest of any. Both additions paid out inside a day. Watch it; do not cut it.

## Reallocation moves

1. **Read the unread games against Pagat, as corpus work, one game at a time.**
   Epic #445 already describes this sweep and already owns the class; it needs
   scheduling, not an instrument — `docs/games/**` is Lane C, so an agent takes
   one game end to end and no harness gets built. Six games carry a filed
   discrepancy; the remainder is unmeasured. This now **outranks adding a 32nd game**
   (verdict 4's move 3): widening a corpus whose existing members are not the
   games they claim to be is the lower-leverage half of corpus work, and every
   proof and every grid's expected column is taken against these files.

2. **Rank the seven ready R1s** — done below, and the general point outlives the
   edit: #143's prose is the ranking function, so an item that names R1s by
   number goes stale the moment an R1 arrives. Item 1 now names the shelf by
   derivation instead.

3. **The orphaned-surface question, second offering** (#378, #379, #383, #384,
   #385, #386 — one answer, six tickets). Raised in verdict 4 only; untouched
   since. Re-offered without restating the argument. **A third offering is the
   operator conversation this skill's stopping rule requires, not a verdict.**

## Applied in place

- Issue #143: item 1 rewritten — the R1 shelf is now stated as a derivation
  (`tools/ready-front.sh`, R1 rows) with the seven previously unranked R1s named,
  so the rank the sweep computes matches the shelf that exists; item 2 carries
  epic #445's unread-games sweep ahead of the next new game, with verdict 4's
  Euchre/Spider unblocks kept behind it; item 7 ratifies the ledger migration as
  bounded at what is done, the remainder recorded as legacy-format.
- No issues filed, no machinery added, no doctrine amendment drafted.
