# Direction review — 2026-08-09

Second run; overdue by its own cadence (46 merges since #172, not ~10).
Window: 2026-07-29 → 2026-08-10, #184 first, #308 last by merge time.
Next window starts after #308's merge.

## The shape

**The demanded witness surge happened, paid, then stopped cold.** First
half (through 2026-08-05), 26 PRs: 8 witness/execution — the second
library (#187), two corpus Hold'ems (#195, #241), Salvo (#190), the LLM
eval harness, Kuhn with an exact solver, the N=1500 replication (#185,
#217, #234, #245). Verdict 1's pass condition met three times over, and
the witnesses did what witnesses do: found #197 (R1, closed next day)
and #198 (R1), and bought epic #248 — eight R2 constructs mapping the
language frontier. The healthy shape, exactly as ordered.

Then the tail (2026-08-06 on), 20 PRs: 14 process/infra — licenses, CI
pool, README (grant-driven, one-time), then the Operating Harness
buildout (charters, cadences, Dispatcher, Warden, pr-watch) — 6
language-surface, 0 witness. The finding stream followed: the harness
spawned five R4s about itself (#278, #279, #291, #303, #305) and
consumed two corrective PRs (#307, #308) inside its first 48 hours —
verdict 1's machinery-examining-machinery signature, transposed to
process. The mitigating fact: the tail's two Lane A changes (#268 mode,
#301 teams) yielded four fresh R2s (#281, #282, #292, #297) — the tail's
R2 decline is work-mix, not surface soundness. Language work still pays
R2s; the surface is not close to sound.

The harness is a deliberate operator bet that process throughput
converts to execution throughput. The bet is placed; its first round
produced one real R2 close (#155 via #301). Conversion is now the whole
question, and this verdict points the converted capacity.

## The other checks

- **Aging (check 3): nine open R1s, and all five that verdict 1 said
  "must not slip" slipped the full window** (#105, #126, #154, #157,
  #158 — 15 days old). The worst is newer: #198 — after a bet, every
  3+-seat poker street asks seats from the round's leader instead of
  past the aggressor, so the deciding player's information set is wrong
  in four corpus games. That is the project's own currency, open 10 days
  while 14 process PRs merged. #25 (the oldest R2, the OpenSpiel
  promise) reached 37 days — its second verdict. Both are on their
  second flagging; standing at a third review makes them an operator
  conversation, not a third verdict, per this skill's stopping rule.
- **Inflow vs closure (check 4):** ~76 filed vs 26 closed, net +50.
  The 26: 12 renames/tech-debt (epic #204 drained to its capstone,
  now in flight as PR #309 — the window's model closure), 6 R2 bugs,
  1 R1, 4 harness charters. Classes: #248 (the frontier) 8 in / 0 out —
  not outrun yet, but every child is Lane A, so only the operator can
  move it. #181, verdict 1's outrun-watch: its witness landed (#187)
  and none of its six moved in 12 days — correctly parked now that
  reachability ranks them R3/R4; off the watch. #274 (harness) runs at
  ~1:1 self-sustaining — the class to starve by ranking, below.
- **Doctrine cost (check 5): no amendment demanded.** Verdict 1's
  scaffolding light gate held and did its job — the charters moved at
  low per-PR cost; portfolio share is this review's job, not a gate's.
  The surface-totality framing check paid again (#210's
  accepted-but-ignored find). No gate in the window was all-cost.

## Reallocation moves

1. **The R1 shelf goes to rank 2 in #143**, above every ranked item
   except the in-flight capstone: #198+#237 (one betting-round
   machinery; the `order` ruling is the operator's, the landing is
   mechanical), #192 (announce info-state), the eval-evidence trio
   #199/#219/#220 (the numbers the grant quotes), #105, #126, #157.
   The Dispatcher selects by #143 rank before reachability, so the edit
   is the steering input. Pass condition next review: at least 3 of the
   9 R1s closed.
2. **The operator's next Lane A slot after #309 goes to epic #248, not
   further harness surface.** #249 is the smallest first bite. Pass
   condition: one #248 construct merged or in review by next window.
3. **The harness's own findings rank behind every open R1/R2 for fleet
   time** (#278, #279, #291, #303, #304, #305) — stated in #143 so the
   fleet cannot drift into feeding itself; the operator's own hand
   stays free.

## Applied in place

- Issue #143: R1 shelf inserted at rank 2; the #248 frontier item
  inserted; #155 and #101 trimmed (closed); #297 joined the
  declaration cluster with #298–#300 queued behind it; harness findings
  ranked behind open R1/R2; revision marker updated.
- No issues filed, no doctrine edits, no new machinery.
