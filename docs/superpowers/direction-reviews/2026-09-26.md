# Direction review — 2026-09-26

Ninth run, at the operator's request, with a small window. It covers the
PRs merged after verdict 8's snapshot (PR #748's merge, 2026-09-22 22:59
UTC), selected by merge time. That is #753, #754, #759 and #761, plus
#752, which was verdict 8 itself. Issues are counted from verdict 8's
merge (2026-09-22 23:34 UTC). Measurements were taken on 2026-09-26 at
`085479d6` (PR #761's merge). The next window starts after #761.

## Verdict 8's scorecard

- (1) #281 closed inside milestone 4, with its plant verified: **MET**.
  PR #759 closed #281 and #529. #758 joined the milestone on the
  operator's word and closed with PR #761. The plants for all four
  classes are quoted on #312.
- (2) #612 closed: **MISSED**. Milestone 5's Lease, `claude/issue-751`,
  is 64 hours old with no commit and no PR. `tools/stale-leases.sh`
  reports it reap-eligible. The window's one execution on #612 was a
  finding: a strict witness from PR #759, posted on the issue.
- (3) Tichu's corrected file probed, with the result posted on #703:
  **MISSED**, and the review caused it. The probe lived in a sentence of
  #143 ("probe ... before opening it"), and no issue on the Ready Front
  carried it. The review did not check that its own move was selectable,
  which is the failure its output contract already names. This review
  makes the probe the first step of the Tichu unit.

## The shape

**The window was one Active Epic, it was machinery, and it closed a
designer-facing defect class.** Milestone 4 built a checker refusal (a
rule that reads cards the seat it reaches cannot see) and extended a
proof. That came to two PRs, six Codex rounds and four residuals.
- The refusal is R2 and designer-facing. The proof change is proof
  generality, execution surface by this review's own definition.
- Twelve issues closed and five arrived, so the open count fell from 322
  to 313.
- No corpus game changed in the window. The last new game entered on
  2026-09-06.
- The window leaned machinery-ward because verdict 8 activated a
  machinery epic, not because the fleet drifted. The move that rebalances
  it is a wrong game, and Tichu is next.

## The checks

**(1) Severity mix.** The five issues filed are four R2 and one R3, and
no R4. All five are residuals the hidden-read unit found in executed
probes:
- #755, a branch on concealed cards;
- #756, Old Maid's blind draw, which is Lane A;
- #757, a shuffled face-up pile;
- #758, delegated rules, which closed in the window;
- #760, a `choose` inside a rule, re-asked at every legality
  computation.

Codex ran four rounds on #759 and two on #761 and posted 4 P1s and 7 P2s.
- **The P1s:** three were real silent accepts (a produce payload judged
  at its producer, a consumer written before its producer, a routed
  family credited game-wide). The fourth did not reproduce as silent.
- **The P2s:** six were false refusals. The seventh, on the harness, was
  a true positive for #612.
- **How the repeats ended:** the three look-alike repeats were closed by
  properties generated from the grid (let-, as- and depth-invariance),
  not by more patches. That is the stopping rule's "same class twice"
  case, handled as written.

**(2) Machinery vs witness.**

| PR | What it built |
| --- | --- |
| #759 | A checker Owner Guard plus proof generality |
| #761 | A checker Owner Guard |
| #754 | A retirement (thirteen grammar rows, negative machinery) |
| #753 | Documentation |

No game, differential or playout reach landed. See the shape above.

**(3) Aging.**
- The oldest open R1s are #154 and #158, from 2026-07-26. Both are
  queued in #143 (items 2 and 4), so they are correctly parked.
- #486 has been untiered since verdict 8's reading of it.
- The oldest R2 is #25, the proof-generality track. PR #759's harness
  change serves it without closing it: the proof is still per corpus
  game. Correctly parked.
- No new flag.

**(4) Inflow vs closure, by class.**
- **Hidden reads:** three closed (#281, #529, #758) and three arrived
  (#755, #757, #760). Level, because the unit closed its class and
  filed the neighbouring ones it found.
- **Struct surface:** #693's retirement closed the eight issues it
  blocked (#96, #122, #125, #146, #337, #398, #503, #570), all at #754's
  merge, with no arrival. A class retired outright.

**(5) The doctrine's own cost.** CI's check went from about 12 minutes to
15-16 (15m17s on #761's last head). The cause is the invariance
properties, about 10,000 generated cases.
- They changed outcomes: 23, 18 and 8 red cases on real defects before
  their fixes. So they are not a narrowing candidate.
- The standing cost is real. It belongs to #143's perf item (#158), and
  sampling the generated cases per run, as the golden dial does, is the
  lever.
- No amendment.

**(6) Surface nobody uses.** Twelve of 186 rules and eight of 119
keywords are dead, against 24 rules at verdict 8. #754 retired thirteen.
Every remaining row is a keep decision verdict 7 recorded, or `always`,
which survives only in the rejection that teaches its fix. Nothing new
to decide.

## Active Epics

- **Milestone 4 closed, then epic #312,** in the order #750 requires
  until it lands. The finish line was read against the tree: #281 and
  #758 closed, and the plants were verified (the record is on #312).
- **Milestone 5 (epic #751) stays open and is stalled.** Its Lease is
  commit-free after 64 hours. The Warden reaps it on a second sighting,
  which returns the epic row to the front. Reassigning the unit sooner
  is the operator's call.
- **Milestone 6 opened: Tichu plays its rulebook**, epic #762, due
  2026-10-16.
  - It holds #703 and #721 to #727, and its first step is the probe.
  - The game file is Lane C. A part the probe finds needs engine work is
    Lane B.
  - A part that needs grammar leaves the epic as the operator's step,
    and the epic's body says so.
  - #469, the read that spawned Tichu's parts, is now blocked by #762,
    so the next read (Skat, #728) waits for this one to drain.

## Triage

Issues filed since verdict 8 that no tier covered:

| Issue | Tier | Reason |
| --- | --- | --- |
| #755, #757, #760 | `priority:P1` | Silent defects a designer meets |
| #750 | `priority:P2` | Departure, stated on the issue: it faces the fleet, not a designer, and it takes down the Ready Front |
| #749 | none | Departure, stated on the issue: its fix sits on the 2026-09-07 scoring ruling and likely needs surface, which is the operator's |

#756 is R3 and is not tiered.

## Issue #143

- Tichu left the queue to become milestone 6, and "A checker-green
  sentence that lies" (#588, #628, #629, #691, #539, #581) is its head.
  It is six R2 P1s, all checker fixes, none of them grammar, so it is
  promotable when a slot opens.
- The operator paragraph now names the retire set's engine remainder
  (#378, #379, #383, #384), since its grammar half closed with #693.

After the edits the Ready Front lists #762 first, then the P1 rows. Every
move this verdict names is a selectable row or an operator decision.

## Next window's pass conditions (each selectable)

1. Tichu's probe posted on #703, as the first step of #762.
2. #612 closed, or milestone 5's Lease reaped and retaken.
3. Tichu's Lane C parts closed.

## What a green does not prove

Counts are a snapshot from `gh` at `085479d6`. The window is four
working PRs, so its ratios are anecdotes, not trends: this verdict makes
no share or ratio claim across windows. The PR classification is a
judgment call, and #759 sits on the enforcement/execution boundary.
