# Direction review — 2026-09-06

Sixth run, four windows late: 44 PRs merged since the previous verdict
(PR #468) against a cadence of ~10. Window: every PR merged after #468 —
#470 through #585, 2026-08-29 to 2026-09-06. Issues counted from #470
(the first filed after verdict 5's last derivation). Measurements taken
2026-09-06 at `892ae99`; the window's base is `55ebd86` (PR #468's merge).
Next window starts after #585.

## The shape

**The ordering worked again, and the window's bulk was a rollout that
moved the honest metric by zero.**

Ten R1s closed (#357, #441, #442, #444, #453, #454, #455, #509, #511,
#557), the shelf went from nine open R1s to five, and two of verdict 5's
three pass conditions were met — both of the ones the front could select.
Two new R1s arrived unranked (#486, #546) and the sweep ranked six R3/R4
tickets above them, the same mechanical failure as verdicts 4 and 5,
because item 1 names members by number and the numbers moved. Fixed
below, again; the shelf's first paragraph now says what heads it (a wrong
game) rather than only who is on it.

Thirteen of the 44 PRs are the primitives-block rollout (epic #142 stage
3, #475 to #585). It is language-frontier work — a game declares the
Python it borrows — and it retired no module: `cardlang/runtime/` lost
`coup.py` and gained `salvo.py`, net zero over the whole window. Its
audits filed 38 R3/R4 tickets in nine days. The tracker grew from 199
open to 246 and crossed the 200-row cap of its own sweeps on about
2026-08-29 (#485), so the Warden reported nothing for a week and the two
CLAUDE.md sweeps have been silently truncated since. That cap is raised
in this verdict's PR.

## The checks

**(1) Severity mix — designer-reachable share fell from 64% to 47%, and
the source is the rollout auditing its own new surface, not machinery
auditing itself.** 72 filed: 5 R1 / 29 R2 / 19 R3 / 19 R4. R4 share is
flat (24% to 26%); R3 doubled (12% to 26%). Read by body, the R3/R4 stream
is mostly "the block cannot yet declare X" (#516-#518, #521, #547 —
witness-gated, correctly parked) and "a table or pin has no consumer"
(#528, #540, #580, #571, #574) — the second kind is the kind CLAUDE.md's
tracker section already exempts from filing when it is auditor-only. Not
the drift shape; a filing-rate observation, first sighting, no amendment.

**(2) Machinery vs witness — 12 execution surface, 13 rollout, 5
enforcement, 9 docs, 5 harness.** Execution: #480, #482, #506, #510, #513,
#527, #538, #556, #558, #559, #565, #572. Rollout: #475, #502, #505, #512,
#522, #525, #526, #530, #536, #561, #575, #578, #585. Enforcement: #488,
#491, #500, #519, #564 — and #500 and #519 are walls that answer
designer-reachable R2s (#131, #514), so they sit on either side; the
conclusion does not move. The docs run (#552, #556, #567, #568, #572) is
the window's most under-counted witness: a cold-read poker build and a
designer path that reaches `pyspiel.load_game` from any file are
execution surface for the reader the language exists for.

The exception two verdicts have named has aged again: **no new corpus
game in 34 days** (last: 2026-08-03). Ranked behind #469 on purpose;
unchanged.

**(3) Aging by reachability, read by body.** Five open R1s: #546 (2
days, wrong game behind it — neglected only in the sense that it arrived
unranked; heads the shelf now), #469 (9 days, one game read and recorded
on its thread — working), #486 (8 days, rig-internal, correctly R1,
ranked third), #154 and #158 (42 days, correctly parked, no wrong game
behind them — unchanged from verdict 5). #25 (64 days) is the rescoped
premise verdict 4 recorded; parked. **One flag without a rank:** the
2026-07-25 batch of thirteen R2 checker tickets (#96 to #146) has never
been re-read against the tree since filing, and the old-bug-batch
precedent says a third of such a batch is already fixed. That is a
Warden-shaped read, not a work item, and this review files nothing for it.

**(4) Inflow vs closure by class.** 72 filed, 27 closed, net +45; 246
open.

- **Epic #445 / #469 is 2 of 16 traditional games read** by its own
  thread's count (Oh Hell recorded 2026-08-31; Stud re-read to file #546;
  French Tarot's Excuse read to close #357). The read is working and the
  denominator now exists on the issue, which it did not before.
- **The ledger-format migration went backwards.** Legacy-format modules
  84 at base, **87** at HEAD; current-format 18 to 32. Zero migrated, and
  three modules were **born in the legacy format** this window
  (`tests/test_phase_scoped_reads.py`, `tests/test_primitives_block.py`,
  `tests/test_salvo_primitives.py`) — so item 7's "new modules are born
  in the current format" was false, and #143 now says "either format".
  This is the batch-or-bound choice's **third** window unmade (verdict 4
  offered it, verdict 5 re-referred it). Per the stopping rule it is a
  conversation with the operator, and this verdict does not re-argue it.
- **Epic #248 did not move** (net zero modules; #255 still the cheapest
  tick, untaken). Stage 3 of #142 closed; its last box has two
  definitions that disagree and needs a planning pass before it is
  takeable.
- **The orphaned-surface question is on its third offering** (#378, #379,
  #383, #384, #385, #386 — no comment since 2026-08-25). Same rule:
  conversation, not verdict. #143 names them last so the sweep ranks
  them behind every ordered item until the answer lands.
- **One duplicate**: #485 and #531 are the same sweep-cap defect filed
  four days apart. #531 closed as duplicate; #485 closes with this PR.

**(5) The doctrine's own cost.** No gate consumed effort without changing
an outcome in a way this review can cite. The near-instance is the one
above: the audit's "filed en route" step on the rollout produced R4
tickets on machinery that the tracker section's R4 carve-out says need
no issue, and the tracker's mass then blinded its own hygiene sweep. That
is under-application of an existing carve-out, not a missing rule, so no
amendment is drafted; if the next window shows the same, that is the
second sighting and the amendment is due.

CLAUDE.md: 448 to **451** lines. Flat, after the fastest window on record.

## Reallocation moves

1. **#546 heads the shelf, #469 beside it, and the R1 shelf's first
   paragraph now states the ordering rule** (a wrong game behind it ranks
   first) so the next arriving R1 has a sentence to join rather than a
   list to be missing from. #486 and its blocked sibling #487 ride third.

2. **The rollout's residue is ordered as takeable checker work** — #581,
   #582, #579, #539, #533, #560 — behind the corpus items and ahead of
   the aged declaration-clause pass. Each is met by a designer adopting
   the block `docs/authoring.md` now points at; none asks for grammar.

3. **Two operator conversations, both third offerings, both ended here
   as list items:** the orphaned-surface answer (one question, six
   tickets) and the ledger batch-or-bound choice (with the born-legacy
   evidence above). Neither is re-argued; each is put to the operator at
   the end of this review's turn.

## Applied in place

- Issue #143: item 1 gains the shelf-head rule and names #546, #469,
  #486, #487; item 2's open-discrepancy list is re-derived (Bridge #415,
  Stud #546, 500 #106) and the read record is named as #469's thread;
  item 3 names the census as the metric rather than a count, records
  stage 3 closed and stage 4 as a planning pass, and orders the six
  residual R2s; item 7 records the migration as unmade for the third
  time and moves the choice to conversation; the six orphaned-surface
  tickets move to a trailing section so they rank last. Re-run of
  `tools/ready-front.sh` after the edit: ranks 1-6 are #546, #469, #486,
  (#487 blocked on #486), #154, #158; the six residuals rank 23-28; the
  six orphaned-surface tickets rank 51-56. Every ranked move is a READY
  row.
- `tools/tracker-sweeps.sh` LIMIT and the two CLAUDE.md sweeps: 200 to
  500. The refusal stays loud; the constant just fits the tracker again.
- #531 closed as a duplicate of #485.
- No issue filed. No machinery added. No doctrine amendment drafted.

## What a green does not prove

Every count here is a snapshot from `gh` at `892ae99` on 2026-09-06 and
re-derives from the sweeps CLAUDE.md names. Three numbers are judgment:
the five-way PR classification (#500, #519, #559 sit on a boundary), the
game-module census (by hand: files named for a game under
`cardlang/runtime/`, less engine modules), and "games read" on #469
(counted from its thread and from the issues that cite a live fetch).
Nothing here quotes a Pagat hit rate; a read that finds a game correct
files nothing and comments once.
