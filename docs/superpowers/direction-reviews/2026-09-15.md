# Direction review — 2026-09-15

Seventh run, one window late: 24 PRs merged since the previous verdict
(PR #586) against a cadence of ~10. Window: every PR merged after #586 —
#587 through #660 by merge order, 2026-09-06 to 2026-09-15. Issues
counted from #587. Measurements taken 2026-09-15 at `a48e1107` (PR
#660's merge); the window's base is `70959a1` (PR #586's merge). Next
window starts after #660.

## The shape

**The instrument found its first whole wrong game, and the shelf had no
row to put it on.**

The Pagat read (#469) reached Pinochle on 2026-09-15 and filed nine R1s
from one game (#672 to #680): played at a tenth of its size, a passed
seat winning the contract, the bidders losing a game they won, two
hands in five thrown out. That is the read doing exactly what verdict 5
built it to do. Every one of the nine arrived unranked, and so did the
window's other eight R1s — seventeen R1 rows with no rank, more than
the whole shelf verdict 6 ranked. Five more arrived while this verdict
was being written (#685 to #688 from the same read, #692 from the
table), unranked until its second hand edit. The R1-arrives-unranked defect has
now struck a **fourth** time, and verdict 6 named the fourth strike as
the amendment conversation: the shelf wants deriving from the label,
not from prose. The exact edit is drafted below and put to the
operator; #143 is hand-fixed once more in the meantime.

The window's bulk was execution surface for the reader the language
exists for: seven PRs on epic #613 (a designer plays a seat: `demo
--view`, `--at`, a refusal that names its line, a brace that names its
block), three corpus games in two days after 34 static days (Scopa,
Five-Card Draw, Five-Card Stud), and one construct pair (#630, #659)
that let cribbage's show be written in the language. Three PRs were
machinery, all ruled or R4. The ratio leans the way CLAUDE.md says it
should.

## The checks

**(1) Severity mix — designer-reachable share rose from 47% to 75%, and
the R1s come from witnesses, not from machinery examining itself.** 71
filed: 16 R1 / 37 R2 / 8 R3 / 9 R4 (plus epic #613). R1 sources by
body: nine from the Pinochle read, two more from Stud (#636, #650),
three from the view work meeting pyspiel (#592, #596, #612), one
message (#643, R1 by reach), one rig limit (#662). R4 share 26% to 13%;
the "filed en route on machinery" stream verdict 6 watched for a second
sighting did not recur, so no amendment there. Codex over the window:
35 rounds, 7 P1, 44 P2. Every P1 landed on an execution or design PR
(#608, #622, #630, #647, #663). The three machinery PRs (#590, #654,
#660) took 16 of the 35 rounds and 22 of the 44 P2s and produced no P1
— check 5 reads that.

**(2) Machinery vs witness — 14 execution, 7 docs, 3 machinery.**
Execution: #601, #608, #609, #611, #625, #630, #634, #637, #647, #659,
#661, #663, #667, #669. Docs: #587, #597, #622, #633, #645, #652, #654
(#654 deletes grammar — a shrink, counted with docs because its
substance is the ruling that scoring has no constructs of its own).
Machinery: #590 (the ledger batch, ruled), #618 (comment hygiene), #660
(the dead-surface report — check 6's instrument, and the one machinery
PR that adds a reader for this review rather than a gate). The corpus
exception two verdicts carried is closed: last new game 2026-09-06, and
the corpus is 34 files.

**(3) Aging by reachability, read by body.** Twenty-seven open R1s at
the time of the second #143 edit; every one on the front carries a rank.
- #154, #158 (51 days): correctly parked, unchanged since verdict 5.
- #546 (11 days): **not on the front at all** — it is blocked by #648,
  an R2 filed on 2026-09-07 that nobody ranked, so the shelf head
  verdict 6 named has been unselectable for a week. Neglected by
  mechanism, not by choice; #648 is named beside it now.
- #486 (17 days): ranked third for two whole windows and untaken, no
  comment. Read by body it is still R1 and still rig-internal. This is
  the flag verdict 6's age rule exists for: either it is parked, and its
  thread should say so, or it is the front's most-ignored top row.
- #636, #650 (8 days): a wrong game behind each (Stud), unranked until
  now — the same mechanism as #546.
- #592, #596, #612 (8 days): the play epic's own findings, in work.
- **Second offering of a flag:** the 2026-07-25 batch of R2 checker
  tickets (#96 to #146) has still never been re-read against the tree —
  33 of its 42 open members have zero comments since filing. Verdict 6
  flagged it and filed nothing; this verdict does the same, and check 6
  below closes six of them a different way. A third sighting is a
  conversation.

**(4) Inflow vs closure by class.** 71 filed, 10 closed, net +61; 304
open (from 246).
- **Epic #445 / #469 is 3 of 16 traditional games read** on its own
  thread (Oh Hell, Pinochle; Stud by its re-read). The read outruns the
  fix by design — one read filed nine — and the fix is a Lane C corpus
  PR nobody has opened. That is not a refactor being outrun; it is the
  front not ranking what the read finds, which is the shape above.
- **Epic #613 (play)** closed four (#594, #614, #615, #620) and holds
  two (#616 leased, #617), with #682 arriving. The fastest class on the
  tracker.
- **The cold-read message class** (#604, #606, #610, #631, #632, #641,
  #642, #657) arrived from three cold reads and closed zero. It has no
  owning refactor; each is a diagnostic's wording. Named, not ranked
  above the shelf.
- **Epic #248 moved the wrong way**: game modules under
  `cardlang/runtime/` 17 to 18 by this verdict's hand list (Scopa
  arrived with `scopa.py`); `cribbage.py` shrank 127 lines under #659
  and stays. #255 is still the cheapest tick, still untaken.
- **The ledger migration is done** (#590): zero legacy-format modules
  (the anchored grep's one hit is a Python variable, not a ledger row),
  121 current-format. Pass condition 4 met; the conversation is closed.
- **The orphaned-surface ruling (2026-09-06) has been executed on zero
  of six** (#378, #379, #383, #384, #385, #386). Not re-offered; a status
  line. Check 6 adds a second retire set under the same ruling.

**(5) The doctrine's own cost — the review gate on R4 machinery.** PR
#660 took seven Codex rounds and thirteen P2 findings to land a
report-printing tool; a whole column was withdrawn on the fifth round
after the findings kept moving between classes. #654 took five rounds
and five P2s; #590 four and four. None of the sixteen rounds found a P1;
the window's seven P1s all sit on execution PRs that took one to three
rounds each. The desk holds the operator's stopping rule for this in
memory — a round of only R4 findings ends the requests — and doctrine
does not carry it. Drafted below as the second amendment.

CLAUDE.md: **451** lines, unchanged. Flat for a second window.

**(6) Surface nobody uses — `python -m tools.dead_surface` at
`a48e1107`: 24 of 199 rules, 12 of 124 keywords.** Each row decided
under the orphaned-surface ruling; the retire set is one Lane A change
(operator + Language Owner), filed as one issue so the front can carry it.

| Rows | Decision | Why |
|---|---|---|
| `agg_subset_sum`, `agg_subset_order`, `sq_all` | keep | landed 2026-09-08 with #630/#659 ahead of a game; closed-family folds whose siblings are in use (register-symmetry ruling); not re-offered |
| `q_all_rank`, `q_all_suit`, `q_any_domain`, `q_count_domain`; `ranks`, `suits` | keep | the quantifier family's unused arms; siblings live; same ruling |
| `rule_override`, `rule_remove`, `override`; `sel_random`, `random` | keep | closed families (`+`/`-`/`override`, `chosen`/`random`) with a live sibling |
| `vis_clause`, `visibility` | keep | reservation by rejection for `open-questions/move-level-visibility.md` |
| `divided_by_rounded_down`, `down` | keep | #249's route names ceiling division; the floor twin ships with it |
| `card_values` | keep | the only rule that spells it is a reject twin; refusal machinery, correctly kept — a row to read, not to act on |
| `order` (the auction form's `order NAME` option) | **retire** | an optional clause no game writes |
| `type_def`, `struct_field`, `struct_lit`, `field_init`, `derived_block`, `derived_field`; `type`, `derived` | **retire** | user struct types: 34 games, none needs one; #122 asked for a Bridge/Oh Hell witness and both games exist without it; closes #96, #100, #122, #125, #146, #503 |
| `define_def`, `define` | **retire** | closes #398 |
| `actions_where`, `actions` (the only value of `demands:`) | **retire** | closes #626 if `demands:` leaves with it |
| `move_in` | **retire** | a transfer with no source zone |
| `named_arg` | **retire** | closes #337 |
| `players_range` | **retire** | closes #570 |
| `require_optional` | **retire** | — |

## Reallocation moves

1. **The nine Pinochle R1s head the shelf as one cluster, then the Stud
   cluster with its blocker named** (#648 ranks beside #546 and ahead of
   it), **then #469 beside them.** Applied in #143.

2. **The view trio (#612, #592, #596) ranks next**, ahead of the rig
   pair (#486, #662): it is what `demo --view` and the play session stand
   on, and the play epic is the class that is actually closing.

3. **The dead-surface retire set is one Lane A issue, ranked last** with
   the six from 2026-09-06 — it closes eleven tracker rows on merge and
   is the operator's button either way.

## Two amendments, drafted for ruling — not landed

**A. The shelf derives from the label.** Fourth strike; verdict 6 named
this as the conversation. `tools/ready-front.sh` line 89:

```
sort_by([(.rank // 999999), .reach, .number])
```
becomes
```
sort_by([(if .reach == "R1" then 0 else 1 end), (.rank // 999999), .reach, .number])
```

with the header comment at lines 49-50 reading "R1 rows rank ahead of
every other row by label; within a reach class, first-appearance rank of
`#N` in the ordering issue's body, then number", #143 item 1's sentence
"The sweep ranks by first mention in this body" becoming "The sweep ranks
every R1 row ahead of every other row, by label; within the shelf, by
first mention in this body, then by number", and `docs/harness.md`, "The
Ready Front", gaining one clause after "It annotates each Ready issue
with its #143 rank": "and ranks R1 rows first by label, so a wrong game
never waits on a hand edit". Cost, stated: an R1 by reach with small
worth (#643) outranks every R2 — decisions.md already rules that the tag
ranks reach, not worth. A blocked head still needs its blocker named by
hand (#546/#648); the sweep cannot see that.

**B. A round is requested to find defects, not to exhaust them.** In
`docs/harness.md`, "Review threads", after "Rounds are the author's":

> **A round is requested to find defects, not to exhaust them.** On a
> change whose findings so far are all R4 — tooling, doctrine text,
> machinery on machinery — the author requests at most one further round.
> Findings after that are answered as ledger sentences (what the change
> does not prove) or filed issues, never a new round; the threads still
> close before merge. A P1 or a designer-reachable finding restarts the
> count.

Evidence: #660, #654, #590 above.

## Applied in place

- Issue #143: item 1 names the twenty-two arriving R1s in the order
  above, with #648 beside #546; item 2's discrepancy list re-derived (Bridge #415,
  Stud #546/#636/#650, Pinochle #672-#680, 500 #106) and the thread's
  count updated; item 3 records the census moving to 18 and why; the
  trailing ruled section names the retire issue. Re-run of
  `tools/ready-front.sh` after the edit: ranks 1-13 are the thirteen
  Pinochle R1s, then #648, #636, #650, #469, the view trio, #692, #643,
  #486, #662, #154, #158, #281; #693 ranks last of the ordered rows; no
  R1 on the front is unranked, and every ranked move is a READY row.
- One issue filed: #693, the dead-surface retire set (Lane A).
- No machinery added. Two amendments drafted, neither landed.

## Next window's pass conditions (each selectable)

(1) #648 closed and #546 with it; (2) three of the nine Pinochle R1s
closed on Lane C; (3) #486 taken, or its thread says why it is parked;
(4) #693 merged. And the two amendments ruled either way.

## What a green does not prove

Every count is a snapshot from `gh` at `a48e1107` on 2026-09-15 and
re-derives from the sweeps CLAUDE.md names. Judgment calls: the
three-way PR classification (#654 and #660 sit on boundaries); the
game-module census is a hand list of files named for a game under
`cardlang/runtime/`, and it counts 17 at the base where verdict 6's hand
count said 16 — the method, not the tree, moved by one; "games read" on
#469 is counted from its thread. Codex counts read P-badges on review
comments by the bot's login and miss any finding posted as a plain
comment. Nothing here quotes a Pagat hit rate.
