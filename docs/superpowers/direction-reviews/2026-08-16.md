# Direction review — 2026-08-16

Third run, on cadence. Window: 2026-08-10 → 2026-08-16, #308's merge first,
#352 last. 21 PRs. Next window starts after #352's merge.

The operator asked for this one himself — "lots of Codex feedback has
historically meant we're missing something" — so it opens on that question.

## The shape

**Two tracks, both deliberate. Neither drifted; the price of one only became
legible at the end.**

The operator's track did what verdict 2 ordered and more. Six PRs put three
constructs into the language — rounded division (#340), card points as a game
clause (#344), the ring search (#347) — retiring about thirty Python Primitives
across the corpus, nine games at a stroke in #344's case. Verdict 2's pass
condition 2 (one epic #248 construct merged or in review) is met three times
over. That track also paid the findings: #335 and #336 (R2) plus #337, #338 and
#339 came out of #340 alone. **Language work still pays designer-reachable
findings, so the surface is not sound yet — and that is the healthy signal, not
a complaint.**

The agent track spent eight PRs closing epic #204's capstone and seven on
harness surface, and closed **zero R1s and zero R2s**. That was an instruction,
not drift: the operator's standing direction was that the renaming pass closes
out before anything else. It closed today. So this verdict's job is not to ask
why that happened, but to say where the freed capacity goes now the instruction
has expired.

**What the pass cost, stated plainly, because it answers the operator's
question.** #214 was scoped mechanical, zero-behaviour, R4. With #321, the retirement
sweep it depends on, it ran **6,199 changed lines across eight PRs** — 38% of
the window; #332 alone took ten Codex findings over three
rounds, every one in its own additions; and the artifact class had a measured
defect rate in *both* directions — five wrong glossary entries and ten wrong
links. Its designer-facing yield was one R2 (#329). The Codex volume was not the
disease. It was the symptom of a **cost-model error**: linking a term is a claim
about code, claims about code that live outside the code cannot be checked by
the thing they describe, and #214's own D4 rules that domain review-only. An R4
pass was priced as clerical when it was judgment work throughout. Priced
correctly at ~8 PRs of judgment, it would have queued behind the R1 shelf.

The pass was not worthless — it found the prose-narrower-than-its-registry
class, five wrong entries, and one real diagnostic defect — and it is now done
and cannot recur. The lesson is the pricing, not the doing.

## The checks

**Aging (3) — pass condition 1 missed, 0 of 9.** The same nine R1s are open,
now aged 14–22 days. #198 is 17 days old on its **third** flagging; #25 is
**43 days** on its third. This skill's stopping rule says a verdict recurring
twice unactioned is an operator conversation rather than a third verdict, so
both move out of the moves list and into the conversation below.

**Two R1s had no lane excuse, and this is the window's sharpest finding.**
#199 and #219 live in `experiments/**`, which the Merge Lane table puts in
Lane C — agent-mergeable end to end, no operator button anywhere in the path.
They sat 14–16 days while agents merged R4 machinery. Their trio sibling #220
is the same path and the same age, one rank down at R2. Every other R1 is Lane B,
where an agent does the work and stops at the button — and the window closed
with **zero staged PRs waiting on that button**. So neither lane authority nor
operator merge attention was the constraint. The work was not taken. Verdict 2's
move 1 (rank the R1 shelf at #143 rank 2) was the right instrument and had no
effect, because ranking steers selection only among items something is
selecting.

**Inflow vs closure (4):** ~22 filed, 6 closed, net +16. Of the six: four R4,
one R3, one epic. Zero R1 or R2 closed in 21 merges. Epic #248 is the exception
that proves its track works — three of #249's four constructs landed while no
child issue closed, so the tracker *under*-reports that progress.

**Severity mix (1):** 6 R2 / 4 R3 / 10 R4 filed. The R2s split three to the
operator's language constructs (#335, #336, #345), two to the vocabulary pass
(#329, #353), one to the proof harness (#325). The R4s are still mostly
machinery describing machinery — #327 (the war-room cannot say what is
installed), #333 (the narrowing pin cannot see a consumed row), #346 and #348
(fuzz-excuse keying), #351 (the glossary's own issue sweep). Verdict 1's
signature persists, and it now lives entirely on the agent track.

**Doctrine cost (5):** one amendment, drafted below, not landed.

## Reallocation moves

1. **The freed agent capacity takes the Lane-C work first.** #199 and #219 are
   R1s, agent-mergeable end to end, and two weeks old; nothing explains them
   being open. **#220 rides with them** — it is R2 rather than R1, but it is the
   third of the eval-evidence trio #143 already ranks together, it lives in the
   same Lane-C path, and taking it alongside costs nothing extra.

   **#198 is not selectable yet, and this verdict does not schedule it.** The
   `order` ruling is its sole blocker and only the operator can make it, so an
   agent pointed at it would select work it cannot land — which is exactly why
   it sits in the conversation below instead. Once the ruling exists the fix is
   mechanical and Lane B: an agent takes it to green and stops at the button.

2. **New corpus games are the fleet's only self-merging witness work, and #25
   is the tax on them.** `docs/games/**` is Lane C. A new game is execution
   surface, it is what CLAUDE.md says finds what enumeration cannot, and an
   agent can merge it. But every game added since #25 was filed paid the exact
   hand cost #25 exists to remove — a registry row plus a bespoke proof module,
   now 31 of each. **#25 has been un-done twenty-one times over.** That is the
   argument for it, and it has never been made because the issue's own body
   still describes a ten-game registry and a `tests/test_openspiel_ready.py`
   that no longer exists.

3. **Nothing new is filed by this review.** #351 and #353 were filed today in
   the course of closing #204 and are already ranked by their tags.

## Doctrine amendment (drafted, not landed — a skill edit is the operator's)

Gate 3.5 already routes an R3/R4 whose fix is "more than small" to
record-and-file. It did not fire on #214 because an approved multi-phase issue
re-enters planning as "execute phase N", and every phase is small. The
narrowing states what the gate does *not* license, and adds no checkpoint:

> In `.claude/skills/cardlang-planning/SKILL.md`, Gate 3.5, appended to the
> paragraph ending "...not the fix.":
>
> An approved issue's R-tag is authority to do the work; it is never evidence
> that the work still in front of you is proportionate. A phase of a large
> R3/R4 issue is measured against what that issue has cost so far, not against
> the phase.

## Operator conversation (not verdict items)

Both have survived three reviews, on different grounds. Conflating them would
weaken both.

- **#198 is unactioned work blocked on a ruling only you can make.** Four
  corpus games carry a wrong information set at the deciding seat. The `order`
  ruling is the sole blocker; the landing is mechanical after it. Make the
  ruling, or descope the issue — either is an answer, and its absence is what
  has cost seventeen days.

- **#25 is a tracker-hygiene failure wearing an allocation failure's clothes.**
  Its ask is *unmet and correct*: the proofs still cannot run on an arbitrary
  path without a registry edit. But its Summary describes a tree that has moved
  on — ten games became thirty-one, and the file it names was replaced by 31
  per-game modules over a shared harness. An agent reading it finds a world
  that does not exist and bounces, which is a sufficient explanation for three
  flaggings with no taker. It needs its body refreshed against the tree before
  it is ranked again; re-flagging it a fourth time without that would be the
  review failing at its job.

## Applied in place

- Issue #143: see the revision note there.
- No issues filed, no machinery added, no doctrine landed.
