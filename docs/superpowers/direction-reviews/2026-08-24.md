# Direction review — 2026-08-24

Fourth run, two windows late. Window: every PR merged after #352 except #354
(the previous verdict itself) — #349, #355, #362, #365, #367, #368, #369, #371,
#372, #377, #381, #388, #394, #396, #399, #400, #404, #405, #413, #420, #421,
#423, #426. 23 PRs, 2026-08-16 to 2026-08-24. Next window starts after #426.

## The shape

**The R1 shelf broke, and the language track now manufactures its own R4
backlog. Those are the window's two facts and they point opposite ways.**

Verdict 3's move 1 landed but for its rider. #199 and #219 — the Lane-C R1s
that sat 14-16 days with no structural excuse — closed today; **#220, the trio's
third member that verdict 3 attached to them, is still open** (R2, filed
2026-08-02). #198, the R1 that survived three flaggings waiting on an operator
ruling, got the ruling and closed today too (PR #426, ten game files). #105 advanced and paid on the spot: PR #421's
`PlayoutPolicy` reached Spades' +500 on 27 of 30 seeds where the uniform chooser
reached 0, and in reaching it found that Bridge scores slams its own bid cap
makes unreachable (#415), that the pin written to catch a leak had restated the
bug as its assertion, and #416 besides. **That is "execution finds what
enumeration cannot" paying out inside a single PR, and it is the window's
strongest evidence for where the next hour goes.**

So verdict 3's diagnosis — "ranking steers selection only among items something
is selecting" — is answered. The front took the ranked work as soon as the
renaming instruction expired. That question is closed; do not re-ask it.

**What replaced it is new, and it is not drift.** The window filed 51 issues
across 23 PRs — 2.2 per PR against verdict 3's ~1.0 — at an unchanged
designer-reachable ratio (15 of 50 R1/R2 = 30%, against verdict 3's 30%). The
ratio held; the volume doubled. And a derived slice of the new R3/R4 names its
own cause: **#378, #379, #383 and #384 each say a guard, a proof half, a facts
field or a published channel lost its last consumer when #250's migrations
retired the Python that read it** (PRs #377 and #381 by name). #385 and #386 are
the sibling shape from the same wave — a census arm and a recorded reason that
cannot fail. Three of the four say outright that they should be decided
together.

This is a better problem than verdict 1's signature and needs a different
answer. Verdict 1's R4s were machinery describing machinery. These are machinery
**stranded by success**: the migration works, and nothing in the retirement
sweeps what the retirement orphans.

## The checks

**(1) Severity mix.** 1 R1 / 14 R2 / 18 R3 / 17 R4 filed, 51 total, 1 epic. The
single R1 — #357, French Tarot forcing a trump after a led Excuse against what
Pagat says — came from language work on a corpus game, which is the healthy
source. R2s split 7 to the language/corpus track, 5 to the experiment rigs, 2 to
the ledger track. **Language work still pays designer-reachable findings, so the
surface is still not sound — the same healthy signal verdict 3 recorded.**

**(2) Machinery vs witness — the healthiest split any window has shown.** Of 23
PRs: 8 language/corpus (#349, #365, #371, #372, #377, #381, #388, #426), 3
experiment/witness (#420, #421, #423), 1 checker (#367) — **12 built execution
surface**. Against 7 ledger-and-prose machinery (#394, #396, #399, #400, #404,
#405, #413) and 4 harness (#355, #362, #368, #369). Verdict 3's window had the
agent track at zero R1/R2; this one has it closing three R1s.

The exception worth naming before it is discovered later: **the corpus has not
grown in 21 days.** Last new game 2026-08-03 (holdem-heads-up); 31 games, and
every construct this window landed was exercised against games that already
existed. The language deepened; it did not widen. Defensible while epic #248
drains — but it is a choice, and it should be a said one.

**(3) Aging by reachability.** 7 open R1s, down from 9: three closed, one new.
Oldest are #105 (30 days, now partly paid), #126, #154, #157, #158 (29-30 days),
#192 (25), #357 (8). **None is now unattributed** — #154 rides #143 item 4, #158
item 6, #192 is epic #312's. The R1 shelf is no longer the allocation failure it
was for three reviews.

The aging failure moved. **#25 is 51 days old on its fourth flagging.** Verdict
3 escalated it with one requested action — refresh its body against the tree —
and nobody did; its last edit was 2026-08-11, before that verdict. So this
review performed the refresh rather than flagging it again, and **the refresh
found a better explanation for three failed flaggings than staleness.**

Two thirds of what #25 asked for are gone. The hardcoded ten-game `GAMES` dict
it was written against no longer exists — `cardlang/openspiel/registry.py`
derives `GAMES` from `docs/games/*.cardlang` by glob, so dropping a file in
registers it with the adapter automatically. And the half that made #25
interesting, generalizing the proofs, is now **#325**'s workstream, specified
far better than #25 ever did (emission-site registry, constructive world
generator as a fold over the classification, the solved-partition miniature
first). **An agent who read the tree correctly would have declined #25 — not
because the ask was wrong, but because most of it was either done or someone
else's.** What survives is small and genuinely unmet: no entry point runs the
readiness battery on a path, so every game still needs a hand-authored
`tests/openspiel_ready/test_<game>.py` with a `GameSpec` tuned by trial. The
issue is rewritten to that, retitled, and #281 recorded on it as the constraint
any path-generic runner inherits.

The general lesson is worth more than the instance: **a long-lived issue is a
claim about the tree, and it decays the way any prose about code decays.** Three
reviews read #25's age and ranked it; none read its body against the tree. That
is the same defect class the repo already names for docstrings (epic #406), one
artifact class over. Reading the body before re-ranking an aged issue is part of
doing check 3 properly, not a new obligation on anyone — it cost this review one
file read and saved the fourth wasted flagging.

**(4) Inflow vs closure by class.** 51 filed, 11 closed in the window, net +40;
183 open. (Verdict 3 already counted #204, #214 and #256, which closed on the
day it ran.)

- **Epic #248 advances and its scoreboard understates it.** #250 closed,
  retiring the Trick Order across five games and deleting `doko.py`; the tick
  count is unchanged at 15/31 because a construct touching five games ticks a
  box only where it was the *last* Python standing. Per-game Python modules are
  the honest metric: **16 remain** under `cardlang/runtime/`. The two
  single-item ticks — #255 (bridge: one auction arm, no module at all) and #142
  (coup: one function) — are still untaken and are the cheapest scoreboard moves
  on the board.
- **The ledger-format migration is the class being outrun, and it is outrunning
  itself.** #392's fix has migrated **8 of 94 modules over four PRs** while
  filing 8 issues in the window about its own format. A refactor whose
  self-referential inflow exceeds its per-PR progress is not converging.
  (Measured 2026-08-24 on this worktree: `grep -rl 'covered:' tests/ cardlang/`
  gives 86, `grep -rl 'does not prove:' tests/ cardlang/` gives 8. #392's body
  measured 87 modules carrying a ledger at `591e44f` — a different domain, so
  the command travels with the number.)

**(5) The doctrine's own cost.** Verdict 3 drafted exactly one amendment and it
was not landed — and **the failure mode it names recurred inside the same
window**: an R3 ledger-format sweep entered on an approved ruling, ran four PRs
inside a seven-PR prose-and-ledger track, and has 86 modules left. It is re-offered below unchanged. This is its second
offering; a third would be the conversation this skill's stopping rule requires,
not a verdict.

## Reallocation moves

1. **Answer the orphaned-surface question once; six tickets close together.**
   #378, #379, #383 and #384 (plus siblings #385, #386) ask one question in four
   voices: when a Primitive retires, does the surface that served it retire with
   its guards and proof rows, or stay marked as the sanctioned future path?
   Neither answer is expensive; carrying four undecided tickets is. Whoever
   answers it is also positioned to keep the next migration wave from
   manufacturing another four — but that is a consequence of answering, not a
   new obligation this review imposes.

2. **Batch or bound the ledger migration before another PR of it.** #404's whole
   purpose was calibrating the routing judgment, and it succeeded — so the
   remaining 86 either go in bulk on that calibration, or the migration stops at
   what is done and the rest is recorded as legacy-format. What must not happen
   is another seventeen five-module PRs at R3.

3. **A new corpus game is the highest-leverage unspent move.** The corpus is
   static for three weeks while the language deepened around it. Euchre (the
   bower witness for contextual-rank declaration) and Spider (positional slice
   movement, #111) each unblock a named open question, and `docs/games/**` is
   Lane C — an agent takes one end to end. #25, now rescoped, is the small tax
   on that path and is takeable beside it.

## Doctrine amendment (re-offered unchanged — second offering; a skill edit is the operator's)

> In `.claude/skills/cardlang-planning/SKILL.md`, Gate 3.5, appended to the
> paragraph ending "...not the fix.":
>
> An approved issue's R-tag is authority to do the work; it is never evidence
> that the work still in front of you is proportionate. A phase of a large
> R3/R4 issue is measured against what that issue has cost so far, not against
> the phase.

## Operator conversation

- **#25 comes off the conversation list.** It is rescoped, retitled and
  takeable. If it goes untaken through the next window, that is a selection fact
  for check 3 — not a hygiene complaint.
- **The Gate 3.5 amendment is on its second offering.** One sentence, no
  checkpoint, and the failure it names recurred inside the window it was drafted
  in. If it is not wanted, say so and it comes off the list. A third offering is
  the thing the stopping rule forbids.

## Applied in place

- Issue #25: body rewritten against the tree and retitled. Its registry premise
  is dead (the adapter's `GAMES` derives itself now) and its proof-generality
  half belongs to #325; what remains is a path-generic entry point for the
  readiness battery, with #281 recorded as the constraint it inherits.
- Issue #143: revised — item 1 retitled "the execution-debt shelf" and rewritten,
  since its contents had widened past a title that said R1 (the R2 riders #237,
  #281 and #220 belong to it, ranked by the debt rather than the tag), with #220
  named as its cheapest unfinished item; item 2 carries #25's
  new scope and its #325/#281 neighbours; item 3 carries the honest epic-#248
  metric (16 game modules) and the four-tickets-one-answer orphaned-surface
  decision; item 7 bounds the ledger migration.
- No issues filed, no machinery added, no doctrine landed.
