# Typed amount syntax

**Tier 3 — medium impact, narrow scope.**

`transfer { wood: 2 } from bank to hand[player]` is the current
sketch for "two wood units" as a transfer argument. Curly braces
could collide with block syntax; alternatives include `2 wood` or
`wood × 2`. Pick once a second resource-using game surfaces real
preferences.
