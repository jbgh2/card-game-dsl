primitives_phase_state_zone_read.cardlang:13:53: error: `pinochle_meld_value` reads `pot`, which phase `play` declares as state while this game declares it as a zone — the declaration cannot say which, and an untailed `reads` name classifies against the zones and the GAME's state, so the phase's variable is not what it denotes; rename one of the two (a scope tail does not settle it either — a zone is game-level whichever phase a read names, so one of the two spellings would still be unreachable)
