primitives_phase_state_zone_read.cardlang:13:53: error: `pinochle_meld_value` reads `pot`, which phase `play` declares as state while this game declares it as a zone — the declaration cannot say which, and a `reads` name classifies against the zones and the GAME's state, so the phase's variable is not what it denotes; rename one of the two (a phase-local variable is unreadable by a declaration either way — the row is materialized on every call)
