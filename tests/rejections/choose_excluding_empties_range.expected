choose_excluding_empties_range.cardlang:14:50: error: `choose integer` excludes 3, the only value it can offer (3 .. 3), so the `excluding` clause would always empty the choice — a choice must offer at least one candidate
