primitives_repeated_read.cardlang:13:59: error: `pinochle_meld_value` reads `hand` more than once — a `reads` clause names each declaration at most once, and the repeat would silently replace the first; keep one
