primitives_scoped_read_two_phases.cardlang:14:5: error: `pinochle_meld_value` reads `trump_suit in outer` and `bonus in later`, and `outer` and `later` do not nest — no place in this game runs both, so the entry could never be called; keep one, and pass the other value as an argument or declare it in the game's `state { }`
