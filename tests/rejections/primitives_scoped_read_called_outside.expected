primitives_scoped_read_called_outside.cardlang:19:16: error: `pinochle_meld_value` reads `trump_suit in outer`, so it is callable only where `outer` runs — this call is in `later`, which runs outside it; move the call inside `outer`, or pass the value as an argument
