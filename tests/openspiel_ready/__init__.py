"""The OpenSpiel-readiness proofs, one test module per fully-kernel game.

The shared proof machinery lives in `harness.py`; `test_coverage.py`
keeps the per-game split total against the adapter's registry.
"""
