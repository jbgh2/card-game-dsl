"""The standard library and, until issue #203 rehomes them, the kernel
tables: zone types and projections, board families, round-state fields,
move types, enum namespaces. The native surface's DECLARATION side moved to
`cardlang/builtins/` (issue #202); what belongs here permanently is
`rules.cardlang` and its loader — the layer written in the language."""
